/**
 * Pipeline CRM entry — same-origin `/crm` on the production delta deploy
 * (commit 2ee8f5e lineage: All leads / Job listings / Call list / Hot).
 */
export const CRM_URL = "/crm";

/** Inbox for Book-a-demo requests (API + mailto fallback). */
export const DEMO_EMAIL = "tech@vexecutivesearch.com";
export const DISPLAY_EMAIL = "tech@vexecutivesearch.com";
